.env
.env.*
!.env.example

.envrc
.direnv/
.svelte-kit/
build/
node_modules/

data/
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

*.log
.DS_Store

# Claude Design handoff archives — kept locally, not tracked.
*-handoff.zip

# Python bytecode (the ui-ux-pro-max skill bundles .py scripts whose Python
# runtime drops .pyc files alongside them).
__pycache__/
*.pyc
